node_modules/
dist/

# secrets — never commit; the .example files are the templates.
# Deny everything env-shaped, then allow back only the templates: the old
# list missed .env.local.staging-backup, which reached a public repo with a
# live service_role key in it.
.env*
!.env*.example
!mobile/.env.example

.DS_Store
*.log

# local-Supabase pointers for pre-flip verification (public demo keys, still not committed)
.env.local.localdb
mobile/.env.localdb
