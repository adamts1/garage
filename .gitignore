node_modules/
dist/

# secrets — never commit; .env.local.example is the template
.env
.env.local
.env.*.local

.DS_Store
*.log

# local-Supabase pointers for pre-flip verification (public demo keys, still not committed)
.env.local.localdb
mobile/.env.localdb
